# TCP POW test task solution

## Description

This is a simple TCP server with ProofOfWork DDoS protection.

## How to start

`docker compose build` for building containers.

`docker compose start && docker compose logs -f` for run and watch output.